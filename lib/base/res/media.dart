class AppMedia{
  //const means in runtime it's not change and static means without class instance we can access the variable
  static const _baseImage = "assets/images";
  static const logo = "$_baseImage/logo.png";
}